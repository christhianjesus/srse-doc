/* ECP: FILEname=fig2_14.c */
   #include <stdio.h>

   main( void )
   {
       int X;
       double Y;

       printf( "Enter an integer and a real: " );
       scanf( "%d %lf", &X, &Y );
       printf( "You entered %d and %f\n", X, Y );
   }
