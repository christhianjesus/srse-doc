/* ECP: FILEname=fig2_3.c */
   /* This comment ends early on the next line.
    * This is not in the comment!
    * Neither is this; the next line is also an error.
    */

   #include <stdio.h>

   main( void )
   {
       printf( "Testing 123\n" );
   }
